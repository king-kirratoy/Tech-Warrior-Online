# Tech Warrior Online — OVERVIEW

> A browser-based top-down mech shooter built with Phaser 3.60.0. Players choose a chassis, build a loadout in the Hangar, then deploy into wave-based combat. **Warzone** is a roguelike run-and-die loop (`_gameMode='simulation'`); **Campaign** is persistent with XP/levels/missions/shop (`_gameMode='campaign'`); **Multiplayer** is real-time PVP via Socket.IO (`_gameMode='pvp'`).

---

## File Map

| File | Purpose |
|------|---------|
| `index.html` | HTML shell with all screen divs and canonical `<script>` load order; no inline scripts or styles. `boss-hud` is a sibling of `round-hud` (not nested inside it) so campaign mode hiding `round-hud` does not affect the boss HP bar. `objective-hud` is anchored `top:12px;left:12px` — 12px from the top-left viewport corner; inner padding (8px top, 18px horizontal) provides additional visual clearance. `boss-hud` is anchored `top:12px` centered — 12px from the top viewport edge; inner padding (10px top) provides additional clearance. HUD weapon-bar row prefix labels updated in v7.46: CPU mod row uses "CPU MOD" (was "CORE"); shield row uses "SHIELD" (was "DEFENSE"). Minimap (`#minimap-wrap`) repositioned in v7.46: `bottom:15px;right:15px` (was `bottom:24px;right:24px`). |
| `css/base.css` | Design tokens (legacy brand + sci-fi UI palette), `--font-mono`, glow/button geometry tokens, `.tw-btn` system, and universal reset. |
| `css/hud.css` | In-game HUD styles: console frame, paper doll, weapon bar rows, and HUD-specific color tokens. `#hud-container` repositioned in v7.46: `bottom:15px;left:15px` (was `bottom:30px;left:30px`). |
| `css/garage.css` | Hangar UI, perk cards, stats panel, dropdown system, and the full `.lo-*` loadout overlay class family. |
| `css/menus.css` | Main menu, pause, callsign, leaderboard, PVP hangar, multiplayer lobby, death screen, warzone perks overlay, drag-and-drop, chassis selector cards, and typography utilities. `.mm-right` uses `justify-content: center` so the pilot stats panel is vertically centered, matching the left side. |
| `js/constants.js` | All immutable game data: chassis, weapons (including `siphon` beam weapon — added to `WEAPON_OPTIONS` in v6.69 so it appears in warzone/PVP dropdowns), shields (11 total: 4 light, 4 medium including `fortress_shield`, 3 heavy), augments (12 total: none + 11 active — 20 removed in v6.68), legs (13 total: 5 universal + 3 light + 3 medium + 2 heavy), cover, arenas, enemy color palettes, restriction Sets (`CHASSIS_LEGS`: light=hydraulic_boost/seismic_dampener/featherweight/ghost_legs, medium=gyro_stabilizer/mine_layer/sprint_boosters/reactor_legs, heavy=mag_anchors/tremor_legs/suppressor_legs/warlord_stride; `CHASSIS_AUGS`: light=threat_analyzer/ballistic_weave/neural_accel/thermal_core, medium=target_painter/overclock_cpu/multi_drone/field_processor, heavy=reactive_plating/war_machine/suppressor_aura/heavy_loader), starter loadouts (v7.07: restored starter shields — light=micro_shield, medium=adaptive_shield, heavy=bulwark_shield; v7.41: shields removed — all chassis now start with shld:'none'; new campaigns start with weapon only), `SLOT_ID_MAP`, and `GAME_CONFIG`. `EXPLOSIVE_KEYS` removed in v6.76 (dual-explosive warning removed). Dead code removed in v7.22: `window.TW = {}` (empty namespace, never populated or read), `ENEMY_PRIMARY` and `ENEMY_ARM_WEAPONS` arrays (enemy weapon selection is done inline in `randomEnemyLoadout`). `LEG_SYSTEMS` section comments corrected in v7.24: `sprint_boosters` comment changed from "LIGHT CHASSIS UNIQUE" to "MEDIUM CHASSIS UNIQUE"; `seismic_dampener` comment changed from "MEDIUM CHASSIS UNIQUE" to "LIGHT CHASSIS UNIQUE" — now matches `CHASSIS_LEGS` authoritative Sets. Bullet speed removed from display strings in v7.30: `ballistic_weave` AUGMENTS entry desc updated (removed "+10% bullet speed."); `ballistic_weave` MOD_DESCS entry desc updated (removed "All bullets travel 10% faster and"); `hydraulic_boost` MOD_DESCS desc updated ("movement speed" → "move speed"). GAME_VERSION bumped to v7.30. Chassis traits overhauled in v7.42: `CHASSIS.light` — removed `dualFire` and `passiveReloadBonus`; new traits are Dual-Wield (same weapon both arms: −15% dmg, −15% fire rate), Lightweight (+15% speed below 50% core HP), Agility (+10% speed +10% dodge with one arm filled). `CHASSIS.medium` — removed `shieldAbsorb:0.60`; added `modDurationMult:1.15` (Mod Specialist +15% mod duration on top of existing −15% cooldown) and `shieldAbsorbBonus:0.15` (Shield Specialist +15% absorb + +15% regen, replaces hardcoded 60% absorb). `CHASSIS.heavy` — removed `noJump`, `passiveRegenRate`, `passiveRegenDelay` (dead fields); `passiveDR:0.15` retained for enemy code compatibility; new traits are Improved Armor (15% DR always), Attrition (+15% DR below 50% core HP), Iron Legs (skip destroyed-leg speed penalty). GAME_VERSION bumped to v7.45. Chassis trait label cleanup in v7.48: no data changes — see `js/menus.js` entry. GAME_VERSION bumped to v7.48. GAME_VERSION bumped to v7.53 (cursor restore bug fix — see `js/menus.js` entry). GAME_VERSION bumped to v7.54 (shield HUD update fix — see `js/menus.js` entry). GAME_VERSION bumped to v7.55 (loot magnet + chassis weapon restriction — see `js/loot-system.js` entry). |
| `js/state.js` | All mutable runtime globals: Phaser object refs, game-mode flags, round/combat state, `loadout`, `_perkState`, extraction state, loot pickups, and leaderboard run state. Dead code removed in v7.22: `_pendingLoadoutTab` (never read or written — `toggleStats()` tab-switch was never wired up), orphaned `_perkState` fields `perfectAccuracy`, `reinforcedCore`, and `fthDmg` (initialized but never set by any perk or read by any game code; also removed from `_resetPerkState()` in `perks.js`). Dead code removed in v7.23: `adaptiveEvolution` field from `_perkState` (set only by the removed `universal_adaptive` perk, never read by game code; also removed from `_resetPerkState()` in `perks.js`). |
| `js/utils.js` | Pure helpers: color utilities, chassis HP calculation, HUD name lookup, and visual FX spawners (damage text, sparks, muzzle flash, debris, footprint). |
| `js/audio.js` | Web Audio API synthesizer with 29 sound functions, throttle/node-cap system, and first-gesture/tab-visibility lifecycle. Siphon beam sounds: persistent hum (`sndSiphonBeamStart/Update/Stop`) with triangle oscillator (80–120 Hz, heat-driven pitch) and 2.5 Hz LFO pulse; overheat buzz (`sndSiphonOverheat`). Dead code removed in v7.22: `case 'missile':` branch in `sndFire()` (missiles are CPU mods, not weapon keys — this branch was unreachable; missile sounds are handled separately in `mods.js`). Added in v7.37: `sndLootDrop(rarity)` plays a rarity-scaled tone on item landing (silent Common, soft clink Uncommon, ping Rare, chime Epic, thud+shimmer Legendary); `sndScrapPickup()` plays a quick sine chirp 1500→2000 Hz on scrap collection. |
| `js/mechs.js` | Mech construction, per-frame visual sync, chassis movement effects, rage ghosts, and spectre clone logic. Spectre clone (`_spawnSpectreClone`): Container-based shadow clone with 16ms drift timer (moves toward nearest enemy at 75 px/s), 1s fire timer (cloned bullets into `bullets` group at 50% dmg), 4s expiry. Tint applied via child iteration (`list.forEach`) — Container has no native `setTint` in Phaser 3.60. Dead code removed in v7.22: `player._siphonLine = null` initialization in `_initPlayerHP()` (leftover from pre-refactor siphon rendering; beam rendering uses `_siphonGfxL`/`_siphonGfxR` in `combat.js`). Shield Specialist absorb updated in v7.42: `_chassisBonus` for Medium changed from hardcoded `0.10` to `CHASSIS.medium.shieldAbsorbBonus || 0.15`. |
| `js/cover.js` | Cover placement, battlefield generation, and cover damage/destruction. Dead code removed in v7.22: `tryPlace` local const inside `generateCover()` (defined but never called — all placement uses `placeAt`). |
| `js/combat.js` | All weapon firing functions, damage processing, shield absorption, area effects, mine mechanics, and siphon beam logic. `createExplosion()` updated in v7.24: added `isPlayerExplosion` boolean parameter (default `false`); when `true`, applies `_perkState.blastMult` as `radius*(1+blastMult)` and `damage*(1+blastMult)`, and applies `kamikazeProtocol` self-damage (20% of explosion damage). GL and RL player-fired explosion calls pass `true`. Enemy explosions, cover destruction, mine explosions, chain reaction, and death blasts do not pass `true`. (`fireSIPHON`, `updateSiphonBeam`, `_clearAllSiphonSlows`, `_clearSiphonSlows`, `_siphonBeamHide`, `_drawSiphonBeam`). Beam rendering: per-arm Phaser Graphics objects (`_siphonGfxL`/`_siphonGfxR`), per-arm heat state (`player._siphonHeatL/R`, `player._siphonOverheatL/R`, `player._siphonFiringL/R`); dual-arm supported — two independent beams drawn when both arms have siphon, slow does not double-stack, heal stacks from both; green heal float numbers above player (throttled 500ms). Layered shield: `_applyPassiveShieldAbsorption` drains Layer 1 then Layer 2 at 1:1 rate; spillover goes to HP; `_applyShieldRegen` regens each layer independently via `player._layer1LastDamageTime` / `player._layer2LastDamageTime`. Tremor Legs: `_triggerTremor(scene)` fires AOE (40 dmg, 120px) while moving, supports all tl* perks. Dead code removed in v7.22: `_createAfterburn(scene, x, y)` (unreachable function — never called anywhere; earlier draft of flame/napalm mechanic superseded by `napalmStrike` perk logic in `fireFTH`). Scrap spawn added in v7.36: `_resolveEnemyDeath()` calls `spawnScrapDrop(scene, _deadRef.x, _deadRef.y)` unconditionally after the death-effects try/catch; swarm boss death path also calls `spawnScrapDrop`. Trait logic updated in v7.42: `passiveReloadBonus` (+20% light fire rate) removed from `fire()` — `_lightReloadMult` removed from reload formula; `_isDualWield` check now Light-chassis-only; `_dualWieldReloadMult` (×1.15 when dual-wielding) added to reload formula for Dual-Wield −15% fire rate. Attrition (Heavy): `processPlayerDamage` applies additional ×0.85 when `player.comp.core.hp / max < 0.5`. Agility (Light): `_agilityDodge` (+0.10) added to `_totalDodge` when exactly one arm is filled. `_applyShieldRegen` now multiplies regen rate by `_chassisRegenMult` (1.15 for Medium) for Shield Specialist. `fireRatePct` reload mult formula corrected in v7.43: `_gearReloadMult = 1 + (fireRatePct/100)` (was `1 -`) so that the documented negative=buff convention is respected — `fireRatePct=-5` now correctly yields 0.95× reload time = 5% faster fire rate. |
| `js/mods.js` | All 18 mod-activation functions, drone builder helpers, and augment/leg application. `activateJump()` guards on `_perkState.jumpDisabled` (v7.24) — returns immediately if `overclock_ii` perk is active. Dead code removed in v7.22: `decoyFireTimer` local variable in `activateDecoy` (declared but never read — actual timer reference is `decoyFireEvent`); `partName` from destructuring `const [partName, part] = parts[0]` in `activateRepair` (changed to `const [, part]`). Mod Specialist (Medium trait) applied in v7.42: all five `_gearModEffMult` calculations now multiply by `CHASSIS.medium.modDurationMult` (1.15) when chassis is Medium, giving +15% duration to EMP stun, Rage, Barrier, Fortress Mode, and Ghost Step. |
| `js/perks.js` | Master perk dictionary, perk pool selection, perk menu render/pick, equip prompt, and per-round perk reset. `_hiddenPerks` Set (11 entries) lists perks retained in the dictionary for future implementation but excluded from the selection pool: `ricochet_rounds`, `smg_ricochet`, `mg_explosive_tips`, `sg_incendiary`, `br_crit_burst`, `hr_concussive`, `sr_double_shot`, `gl_toxic`, `rl_napalm`, `plsm_gravity`, `rail_chain_lightning`. Dead code removed in v7.23: 58 unimplemented perks from the NEW PERKS section (state flags set but never read by any game code) — universal offense/survivability/utility, chassis-specific, weapon-specific, and universal legendary entries. Classification fixes in v7.24: `blast_radius`, `scorched_earth`, `kamikaze_protocol` reclassified from `cat:'universal'` to `cat:'heavy'` (all reference Heavy-only explosive weapons); `overclock_ii` reclassified from `cat:'universal'` to `cat:'light'` (disables JUMP mod which is Light-only). `blastMult` changed to additive stacking from 0 (was multiplicative from 1) — apply functions use `+0.25/+0.50/+0.60/+0.25`; `_resetPerkState()` initializes `blastMult:0`. `heavy_legendary` (Dreadnought) description corrected: "Rage mod cooldown halved" → "Fortress Mode cooldown halved". Flags now read: `blastMult`/`kamikazeProtocol` in `combat.js:createExplosion`, `jumpDisabled` in `mods.js:activateJump`, `scorchedEarth` in `rounds.js:_clearMapForRound`, `heavyDreadnought`/`fmCooldown` in `events.js` effectiveModCooldown calculation. Perk key cleanup in v7.25 (451 perks total): removed 25 non-conforming perks; renamed 72 perk keys so every key matches its `cat` prefix (e.g. `cat:'heavy'` → key starts with `heavy_`, `cat:'sr'` → key starts with `sr_`). All state flags renamed to match new key where clearly derived from old key name; shared flags (blastMult, reloadMult, speedMult, fortress, etc.) left unchanged. Removed 10 perks in v7.26: `rail_capacitor`, `rail_tungsten_core`, `rail_reload`, `rage_feed`, `rage_chain_kill`, `rage_dmg_stack`, `rl2_chain4`, `heavy_siege_mode`, `hr_twin_shot`, `gl_airburst`; corresponding unique state flags (`railTungstenCore`, `rageFeed`, `_rageEndTime`, `heavySiegeMode`) removed from `_perkState` and `_resetPerkState`; `heavySiegeMode` combat branch removed from `combat.js`; `rageFeed` kill-extension block removed from `rounds.js`. `mg_velocity` perk description updated in v7.30: removed "MG bullet speed +30%," — bullet speed is not a moddable stat; description now reads "MG damage +10% at range >350px". |
| `js/enemies.js` | Enemy spawning, full AI state machine, enemy firing, 8 boss spawners, and DOM boss HP bar helpers. |
| `js/rounds.js` | Round init, enemy spawn dispatch, kill tracking, extraction point system, and round banner. `_clearMapForRound()` loot pickup wipe is now conditional on `_perkState.scorchedEarth` (v7.24) — loot orbs persist between rounds by default; Scorched Earth perk destroys them at round start as its tradeoff. |
| `js/hud.js` | All HUD update functions: weapon slots, HP/shield bars, paper doll, minimap, cooldown overlays, crosshair, HUD reset. Siphon heat is shown inline in the L ARM / R ARM weapon bar rows (heat %, color-coded green→yellow→red, OVERHEAT pulse) via `_updateBarRow()` per-arm heat logic; the separate HEAT row was removed in v6.71. Layered shield: `updateBars()` renders a subtle cyan divider at 50% of the shield bar via CSS gradient on the `.wr-bar-bg` container. Dead code removed in v7.21: unused `lootDef` local variable in `drawMinimap()` (loot orb rendering branches on `p.type` directly). Destroyed-limb color fixed in v7.47: `getColor()` (player doll) and `getCol()` (enemy doll) now return `#000000` (true black) at `pct <= 0` instead of `#1a1a1a`; HP > 0 continues to use the green→yellow→orange→red gradient. |
| `js/garage.js` | Warzone hangar UI: dropdown system, slot selection (dual-wield for light; no duplicate weapons for medium/heavy), and full hangar renderer. Dead code removed in v7.21: `_calcWeight()` (zero callers — weight display handled inline in `renderGarage()`). Skill-tree fireRatePct application updated in v7.43: guard changed from `> 0` to truthy check; formula changed to `1 + (fireRatePct/100)` to match negative=buff convention. |
| `js/menus.js` | All menu and overlay logic: main menu, hangar nav, death screen, pause, loadout overlay, hover card system, inventory management, leaderboard, and game cleanup. `_renderGearBonusesPanel()` merges skill tree bonuses into gear bonuses display in campaign mode (v6.94); header shows "GEAR / SKILL BONUSES" in campaign, "GEAR BONUSES" otherwise. Chassis trait bonuses added in v7.45: `_renderGearBonusesPanel()` appends a gold-labeled "CHASSIS TRAITS" sub-section after gear/skill bonus groups — Light shows Dual-Wield penalty (when both arms have matching weapons), Lightweight (+15% Move Spd below 50% HP, always listed), and Agility (+10% speed/dodge, when exactly one arm filled); Medium always shows Mod Specialist (+15% duration/cooldown), Kill Recharge (text line), and Shield Specialist (+15% regen/absorb); Heavy always shows Improved Armor (+15% DR), Attrition (+15% DR below 50% HP), and Iron Legs (text line); `hasChassisTrait` check added to early-exit guard so the panel shows even when there are no gear or skill bonuses. Chassis trait labels shortened in v7.48: Heavy "Improved Armor DR" → "Damage Reduction", "Attrition DR (below 50% HP)" → "Damage Reduction < 50% HP", Iron Legs text row removed (passive with no numeric value); Light "Lightweight (below 50% HP) +15% Move Spd" → "Move Speed < 50% HP +15%"; Medium Kill Recharge converted from text row to stat row ("Kill Recharge" / "-0.5s"); all chassis traits now use short label + numeric value format with no full-sentence descriptions. `_startNewCampaignWithChassis()` updated in v7.45: resets `_skillTreeState.allocated = {}` before calling `saveCampaignProgress()` so new campaigns start with zero skill tree allocations. `_equipItemToSlot()` and `_unequipItem()` call `debouncedCampaignSave()` in campaign mode to persist loadout changes (v6.95). `_equipItemToSlot()` and `_unequipItem()` now call `updateHUD()` after modifying `loadout` so the shield slot display updates immediately (v7.07); equipping a `shield_system` item while deployed also recalculates `player.maxShield`, regen, and absorb in-place. Shield HUD update fix in v7.54: `_equipItemToSlot()` and `_unequipItem()` now also call `updateBars()` immediately after `updateHUD()` so the shield HP bar refreshes without waiting for a combat event; the mid-deploy `player.maxShield` recalc condition extended from `shield_system` only to `shield_system || shield` so equipping a plain shield item mid-mission also updates `player.maxShield` and the bar in real time; same fix applied symmetrically to `_unequipItem()` so removing a shield correctly zeroes the bar immediately. `'dr'` added to `_pctStats` and all `k === 'dr' ? Math.round(v*100)` display branches removed (v7.14): DR now displays as integer `%` like all other percentage stats. Comparison hover card rarity borders (v7.15): each item column gets a full 4-sided `border:2px solid` in its own rarity color; the "CHANGES IF EQUIPPED" diff section has no top border and uses four absolutely-positioned bars (left side, right side, bottom-left half, bottom-right half) to render split left/right rarity colors. Reverted in v7.16: comparison card column borders and diff-section borders now use the neutral `var(--sci-cyan-border)` (`rgba(0,212,255,0.35)`) — same as the outer hover card border — instead of rarity colors; the four absolutely-positioned border bars in the diff section were removed and replaced with a single `border-left/right/bottom` inline style; BACKPACK/EQUIPPED/SHOP source labels now display in the item's rarity color via `rd.colorStr` inline style (rarity name text was already rarity-colored). Item comparison fixes (v7.17): (1) "CHANGES IF EQUIPPED" diff now uses combined base+affix totals per stat — `_newCombined`/`_oldCombined` built by merging `baseStats` with each affix's `stat`/`value` before diffing; previously only `baseStats` was compared so affix bonuses were invisible. (2) Missing comparison card bug fixed — weapon detection in `_showSlotHover` now falls back to `WEAPONS[item.subType]` check so items with legacy or mismatched `baseType` still trigger the comparison; the incomplete `slotMap` for non-weapon items was replaced with a call to `_getSlotForItem()` which correctly maps all types including non-system `legs`, `shield`, `cpu`, `augment`. (3) `bpSlotNames` in `_showSlotHover` extended to include non-system item types. (4) Diff display: inverted stats (`fireRatePct`, `modCdPct`) now flip sign correctly for both buff and debuff cases; `%` suffix added for all `_pctStats` keys. Dead code removed in v7.20: `_showCloudStatusToast()` no-op stub, `_setCompareArm()`, `_buildItemComparisonHTML()`, `_showItemDetail()`, `_renderItemDetail()` (superseded by hover card system), `submitLeaderboardEntry()`, `skipLeaderboardSubmit()` (reference non-existent `#lb-submit-panel`). `#inv-detail-panel` DOM element and `.cmp-panel`/`.cmp-cols`/`.cmp-col` CSS removed from `index.html` and `css/garage.css`. `accuracy` removed from `_statLabels` and `_pctStats` in v7.29. `speedPct` display label renamed from 'Speed %' to 'Move Speed %' (v7.30); `speed:'Projectile Speed'` entry removed from `STAT_DISPLAY_NAMES` and three `if (k === 'speed') return;` guards removed from hover card and diff renderers (v7.30). Legendary trait display added in v7.34: `_buildSingleCardHtml()` and `_mkCol()` in `_buildHoverHtml()` now render a "✦ LEGENDARY TRAIT" section below affixes/unique for items with `item.legendaryTrait`, showing the perk label and description in `--sci-gold` using `.lo-hover-trait*` CSS classes. Boss unique effect display updated in v7.35: unique effects now render using `.lo-hover-trait*` CSS classes (same visual style as legendary traits) with "✦ UNIQUE EFFECT" header instead of the old `.lo-hover-unique*` classes; dead `.lo-hover-unique*` CSS removed from `css/garage.css`. `cooldown` stat display fixed in v7.38: `cooldown:'Mod Cooldown'` added to `STAT_DISPLAY_NAMES`; both `_buildSingleCardHtml()` and `_mkCol()` in `_buildHoverHtml()` now format `cooldown` values with an 's' suffix (e.g. "6.8s") — previously the key was absent from the label map and no suffix was applied, so cpu_system items showed raw ms values. Inverted stat display fixed in v7.40: `_buildSingleCardHtml()` and `_mkCol()` in `_buildHoverHtml()` now check `_hoverInvertedStats` when rendering `baseStats` — inverted stats (`fireRatePct`, `modCdPct`) with negative values display as `+N%` in green (buff), positive values as `-N%` in red (penalty); previously all baseStats used raw `v + '%'` in cyan regardless of inversion. `_renderGearBonusesPanel()` fixed: filter changed from `> 0` to `negKeys.has(k) ? v !== 0 : v > 0` so negative `modCdPct` and `fireRatePct` values appear; display uses the existing `negKeys` Set to produce correct sign and CSS class (`pos`/`neg`); `hasAnyGear` and `hasSkillBonuses` checks updated to `!== 0` so the panel is not hidden when only inverted-stat bonuses are present. Hover card affixes section (v7.43): "BONUSES" eyebrow label added above affix list in both `_buildSingleCardHtml` and `_mkCol` to visually distinguish the single base stat from rolled affixes. Weapon bar display formula fixed: `effRld = fireRate × reloadMult × (1 + gearRldPct/100)` (was `1 -`). Hover card stat display unified (v7.49): base stat rows and affix rows now use identical two-column format — grey label left, green `#00ff88` value with `+` prefix right; separator line (`lo-hover-divider`) and "BONUSES" eyebrow label removed; affix labels parsed with `/^([+\-][\d.]+%?)\s+(.+)$/` to split value token from stat name; inverted stats (`fireRatePct`, `modCdPct`) retain sign-flip and red/green color logic; `fireRate`/`reload` (shown as `N.N/sec`) and `cooldown` (shown as `Ns`) retain their unit suffixes without a `+` prefix. `_renderWeaponBar()` display fixes in v7.52: (1) Fire Rate added to L ARM and R ARM weapon stat cards — computed as `1000/effRld` shots per second, displayed as `FR N.N/s` after DPS using the same inline style; `effFr` is `'0.0'` when `effRld` is zero. (2) CPU MOD cooldown display corrected — was `${w.cooldown}ms` (raw milliseconds), now `(w.cooldown/1000).toFixed(1)s` (e.g. "6.8s"); no game logic changed. Cursor restore bug fixed in v7.53: `returnToHangar()` now sets `document.body.style.cursor = 'default'` alongside the existing `scene.input.setDefaultCursor('default')` call — previously the body cursor was left as `'none'` when returning from a successful campaign mission (no death screen), causing the cursor to disappear over non-button areas of all menu screens; buttons were unaffected because their CSS `cursor:pointer` overrode the inherited `'none'`. |
| `js/loot-system.js` | ARPG loot layer: item generation, rarity/affix system, inventory/equipment management, ground drops (campaign only), unique item effects, campaign cloud save, and save migration arrays (`REMOVED_SHIELDS`, `REMOVED_LEGS`, `REMOVED_AUGMENTS`). `debouncedCampaignSave()` added in v6.95: coalesces rapid `saveCampaignProgress()` calls (skill clicks, drag-and-drop) into one write per 500ms. Boss unique item data convention (v7.13): `fireRatePct` and `modCdPct` in `baseStats` are always negative (penalty offset by affixes); `echo_frame.baseStats.fireRatePct` fixed from +5 to -5; `core_reactor.baseStats.modCdPct` fixed from +12 to -12; `hive_mind` and `core_reactor` modCdPct affix template labels corrected to use `-` prefix. All `ITEM_BASES` and `UNIQUE_ITEMS` `baseStats.dr` values converted from decimal (0.08) to integer (8) (v7.14): consistent with affix storage format; DR now applies correctly in combat via `_gearState.dr / 100`. Dead code removed in v7.21: `triggerEchoStrike()` unimplemented stub (zero callers), `checkMirrorShot()` stub that always returned false (zero callers). Display label rename in v7.28: `modEffPct` affix/template labels changed from "Mod Effectiveness" to "Mod Duration" (stat controls duration of mod activations — EMP stun, Rage, Barrier, Fortress Mode, Ghost Step); `uniqueLabel` for OVERCLOCK item updated accordingly. `accuracy` stat removed entirely in v7.29: removed from `AFFIX_POOL` (was rollable on weapon/arms), from `baseStats` on `stabilizer`, `targeting_array`, `sys_gyro_stabilizer`, `sys_target_painter`, `sys_threat_analyzer`, from `echo_frame` unique affix list, and from `recalcGearStats()` initializer — stat was display-only and never applied to any game logic. Core item generation rewritten in v7.32: `generateItem()` no longer applies item-level or rarity `statMult` scaling to base stats — all primary stats are now rolled dynamically per type: weapons roll `baseStats.dmg` in [floor, floor×1.05] (floor×1.10 for Legendary) where floor is `WEAPONS[key].dmg`; cpu_system mods roll `baseStats.cooldown` in [floor×0.95, floor] (floor×0.90 for Legendary) where floor is `WEAPONS[systemKey].cooldown` (lower is better); armor/arms/shield/shield_system/legs/leg_system roll their HP stat (coreHP/armHP/shieldHP/legHP) in [10,30] ([30,50] for Legendary); augment and aug_system randomly select one stat from a 10-stat pool and roll a value for it. `ITEM_BASES` static baseStats restructured: primary HP stats removed from all item types (rolled at generation); augment/aug_system entries cleared entirely (fully rolled). Secondary stats (dr, speedPct, etc.) retained in `ITEM_BASES` for non-augment types until Session 2 migrates them to affixes. `item.level` retained on all items for shop display and pricing. Campaign mission reward generation in `campaign-system.js` updated: levelMult re-application removed; mission rewards now use rolled base stats with re-rolled affixes at the forced rarity. Affix system rewritten in v7.33: `AFFIX_POOL` replaced by `AFFIX_POOLS` (per item-type arrays of eligible stat keys: weapon/cpu/augment/arms/armor/shield/legs) and `AFFIX_RANGES` (per stat key, separate std and leg value ranges); `RARITY_DEFS` updated to use `affixCount` (fixed: Common 0, Uncommon 1, Rare 2, Epic 3, Legendary 4) replacing the old `minAffixes`/`maxAffixes`/`affixQuality` fields; `rollAffixes(baseType, rarity, excludeStatKey)` rewritten — selects affixes randomly without replacement from the type pool, caps at pool size, rolls values from `AFFIX_RANGES` using the std range for Common–Epic and leg range for Legendary; augments pass their rolled base stat key as `excludeStatKey` so the same stat cannot appear as both base and affix. Campaign mission reward re-roll in `campaign-system.js` updated to match new signature. `LEGENDARY_TRAITS` const added in v7.34: maps weapon subType and cpu/leg systemKey to eligible trait perk key lists; `generateItem()` assigns `item.legendaryTrait` for Legendary weapons, cpu_system, and leg_system items (augments, shields, armor, arms excluded); `recalcGearStats()` applies each equipped item's trait via `_perks[key].apply()` and stores active keys in `_gearState._traitKeys`. Boss unique generation rewritten in v7.35: `generateUniqueItem()` now always produces Legendary rarity, rolls base stats and 4 affixes using the same system as `generateItem()` for Legendary items (no static values, no `levelMult`/`statMult` scaling); unique effects are preserved and displayed in place of a standard trait (no `legendaryTrait` key assigned); `rollBossDrops()` updated to 50/50 drop between the two boss unique items (was 25% legendary / 75% epic); `statMult` dead field removed from `RARITY_DEFS`. Drop system overhauled in v7.36: (1) Item type weights equalized — all 11 type keys (weapon/armor/arms/legs/shield/cpu/augment/shield_system/cpu_system/leg_system/aug_system) now have weight 10 (was weapon:30, others 6–10); enemy-type modifiers (isMedic/isCommander/isBoss/isElite) still double/boost specific types on top of the equal baseline. (2) Tiered drop rates: Normal enemies 50% chance for 1 item; Elite enemies 75% chance for 1–2 items (50%/50%); Bosses 100% guaranteed 2–3 items (50%/50%); old round-scaling formula removed. (3) Rarity weights updated: Common 40 (was 45), Rare 18 (was 15), Epic 9 (was 8), Legendary 3 (was 2); Uncommon unchanged at 30. (4) Scrap coin drop system: every enemy death in campaign spawns a `_scrapDrops` entry — a gold circle (outer ring 0x8b6914, inner fill 0xffd700, radius 8/10) at death position; within 120px the coin magnets toward the player at 200px/s; on overlap (<20px) adds 5–10 scrap to `_scrap`, shows "+X SCRAP" gold float text above the player, plays `sndScrapPickup()`, and saves via `debouncedCampaignSave()`; cleanup included in `cleanupEquipmentDrops()`; per-frame update via `checkScrapPickups(scene)` called from `init.js`. Loot scatter + landing effects added in v7.37: `spawnEquipmentDrop` accepts optional `fromX, fromY, delay` — items arc from death position (scale 0.5→1, alpha 0→1) to a scatter landing 100–150px away (items) or 60–80px away (scrap coins), with 60–80px arc height for items and 40–50px for scrap; up to 8 attempts to enforce 40px min separation between landing spots; per-drop launch stagger is 65ms; `_arcDone` flag prevents pickup until arc completes; bounce tween plays on landing; `sndLootDrop(rarity)` fires at landing (silent for Common, escalating tones for Uncommon–Legendary); `sndScrapPickup()` fires on scrap collection (quick chirp). Pickup notification redesigned in v7.37: `_showLootPickupNotification` now creates a DOM pill element (`position:fixed, right:16px`) — shows item type name only in 14px bold monospace uppercase, rarity-colored 1px border, rounded pill (14px radius), rarity-scaled `box-shadow` glow (none for Common up to double-layer glow for Legendary); notifications stack vertically (newest at bottom), fade in 150ms, auto-dismiss after 1.8s with 300ms fade-out; all DOM elements removed on `cleanupEquipmentDrops()`. Starter item fix in v7.41: `_createStarterItem()` weapon branch updated to match `generateItem()` — base damage is now rolled in [floor, floor×1.05] and `baseStats` contains only `dmg` (legacy intrinsic properties `fireRate`, `pellets`, `speed`, `range`, `radius`, `burst` removed); `equipStarterGear()` shield assignment removed — new campaigns start with L ARM weapon only, `_equipped.shield` remains null. Item generation bug fixes in v7.38: (1) Weapon `baseStats` now only contains the rolled `dmg` value — intrinsic weapon properties (`fireRate`, `pellets`, `speed`, `range`, `radius`, `burst`) were incorrectly being copied from the `WEAPONS` definition into item `baseStats` and have been removed. (2) `cpu_system` `baseStats` now contains only the rolled `cooldown` value in seconds — previous code copied `ITEM_BASES` secondary stats (e.g. `speedPct`, `dodgePct`) into `baseStats` then added `cooldown` on top; `baseStats` is now replaced entirely with `{ cooldown: rolledCd }` for `cpu_system` items. (3) `cpu_system` cooldown roll now divides `WEAPONS[systemKey].cooldown` by 1000 to convert from milliseconds to seconds before rolling and storing (was storing raw ms value, causing display of e.g. "6824.2" instead of "6.8s"). Loot system fixes in v7.43: (1) `rollAffixes` now negates `fireRatePct` and `modCdPct` values before storing (buff=negative convention) and uses `Math.abs(value)` in the label template so displayed labels remain positive. (2) Augment base stat generation (in both `generateItem` and `generateUniqueItem`) negates `fireRatePct` picks. (3) `generateItem` armor/arms/shield/shield_system/legs/leg_system branches now REPLACE `baseStats` with the single rolled HP stat (was adding to a spread of `ITEM_BASES.baseStats`) — each item now has exactly ONE base stat; `ITEM_BASES` secondary stats are no longer carried into generated items for these types. (4) `overcharge` ITEM_BASES simplified from `{ modCdPct:-5, modEffPct:5 }` to `{ modCdPct:-5 }` (one stat). (5) Pickup notification system rebuilt (v7.43): all pills now appear on the LEFT side (`left:12px`); pill background, border, and text are all rarity-colored (rarity colorStr at ~0.12 alpha background, 0.5 alpha border, full text color); glow scales by rarity; `_enqueueNotifPill()` helper manages the shared queue; `_reposNotifs()` slides remaining pills up when one is removed; hard cap of 6 visible pills (oldest evicted); `_showScrapPickupNotification()` added — accumulates rapid scrap pickups into one updating gold pill (`⚙ +X SCRAP`) that resets its dismiss timer on each new pickup and fades out 1.2s after the last collection; scrap pill shares the left-side queue alongside item pills. Changes in v7.45: Shield base HP ranges raised — `generateItem` and `generateUniqueItem` shield/shield_system branches now roll [75,125] for Common–Epic and [125,175] for Legendary (was [10,30]/[30,50]). Changes in v7.44: (1) Scrap coin visual updated — `spawnScrapDrop` now adds a small "S" text label (7px monospace, dark gold `#5c3d00`, centered) on top of the coin circle; label tracked alongside coin/outerRing in all tween and cleanup paths. (2) Scrap magnet removed — `checkScrapPickups` no longer moves coins toward the player; instead coins stay at their landing position and are collected when the player walks within 45px (matching equipment pickup distance), matching the walk-over convention of regular loot. (3) Weapon base stat changed from rolled `dmg` (floor×1.05/1.10) to rolled `dmgPct` (Common–Epic [2,5]%, Legendary [5,8]%); the hardcoded `WEAPONS[key].dmg` is always the combat base; `dmgPct` adds to `_gearState.dmgPct` as a damage multiplier bonus. (4) CPU base stat changed from rolled `cooldown` in seconds to rolled `modCdPct` (Common–Epic [2,5]%, Legendary [5,8]%) stored negative (negative=buff); feeds into `_gearModCdMult = 1 + modCdPct/100`. (5) Weapon `AFFIX_POOLS` changed from `['fireRatePct','dmgPct','critChance','critDmg']` to `['dmgPct','fireRatePct','critDmg']` — `critChance` removed; `dmgPct` can appear as both base stat and affix (additive stack, intentional). (6) Plain `cpu` baseType removed from `_selectItemType` weight table — only `cpu_system` drops, guaranteeing every CPU item has a mod systemKey. (7) Item naming system added: `TIER_PREFIX` const (Common→"Basic", Uncommon→"Enhanced", Rare→"Advanced", Epic→"Elite", Legendary→"Prototype"); `CPU_MOD_DISPLAY_NAMES` const mapping systemKey to display name; `_getItemTypeName(baseType,subType,systemKey)` returns type portion of name (weapon→WEAPON_NAMES[subType], cpu_system→CPU_MOD_DISPLAY_NAMES[systemKey], armor→"Plating", arms→"Servos", shield/shield_system→"Generator", legs/leg_system→"Frame", augment/aug_system/cpu→"Module"); `generateItem` now computes `name = \`${TIER_PREFIX[rarity]} ${_getItemTypeName(...)}\`` after systemKey is resolved (removed static name assignments from weapon and non-weapon branches); `_createStarterItem` updated to use same naming and rolls `dmgPct` [2,5] for weapon base stat. Pickup pill redesigned in v7.50: `_enqueueNotifPill()` accepts optional `icon` parameter — when provided, pill uses flexbox layout (`display:flex, align-items:center, gap:8px`) with icon and text in separate `<span>` elements; `borderRadius` reduced from 14px to 4px (box-like corners); `_showLootPickupNotification()` background alpha raised from 0.12/0.15 to 0.25 (more opaque), `fontSize` reduced from 13px to 12px, and a geometric icon is selected from `_iconMap` by `item.baseType` (weapon→◆, armor/chest→■, arms→▲, shield/shield_system→●, leg/legs/leg_system→▽, cpu_system/mod/mod_system→⬡, augment/aug_system→✦, scrap→⚙) and passed as the `icon` argument; `_showScrapPickupNotification()` updated to match — `borderRadius` 14px→4px and background alpha 0.08→0.25 (scrap pill already had ⚙ in its text). Scrap coin visibility fix in v7.51: `spawnScrapDrop` was passing `fillAlpha=0` as the last argument to `scene.add.circle()`, making the fill permanently transparent; the fade-in tween targeted the display object `alpha` (not `fillAlpha`), so circles were never revealed. Fixed by passing `fillAlpha=1` (opaque fill) and adding `.setAlpha(0)` on both circle objects so the tween correctly fades them in from invisible to fully opaque. Loot magnet added in v7.55: `checkEquipmentPickups` now applies a magnet pull — when the player is within 60px of a landed item (`_arcDone === true`), the item moves toward the player at 200px/s each frame (all visual objects moved by the same (dx,dy) delta; `glow`/`baseGlow` deduplicated via a Set); pickup triggers at `dist < 15` (was `dist < 45` walk-over); mid-arc items are unaffected (guarded by `_arcDone` check). Chassis weapon restriction in v7.55: `_selectBaseItem(baseType, isShop)` gains an `isShop` parameter — when `false` (field drops), weapon selection filters `WEAPON_LOOT_KEYS` through `CHASSIS_WEAPONS[loadout.chassis]` and system item selection is similarly filtered through `CHASSIS_CPUS`/`CHASSIS_LEGS`/`CHASSIS_SHIELDS`/`CHASSIS_AUGS`; when `true` (shop stock), all chassis filters are bypassed so the shop stocks the full weapon and system pool. `rollBossDrops` adds a chassis check on unique weapon drops: if the unique item has `baseType === 'weapon'` and a chassis-restricted `subType` the player cannot equip, the unique is replaced with a regular boss-tier item via `generateItem(round, { isBoss: true })`. `generateItem` passes `!!enemyData?.isShop` as the `isShop` argument to `_selectBaseItem`. |
| `js/enemy-types.js` | Special enemy type definitions (`ENEMY_TYPE_DEFS`), elite modifier system (`ELITE_MODIFIERS`), and all type/modifier lifecycle functions. |
| `js/arena-objectives.js` | Arena layout generators (4 arenas), objective system (4 types), lifecycle functions, and `_arenaState` export. |
| `js/campaign-system.js` | Campaign missions, XP system, mission modifiers, enemy composition, campaign flow, localStorage save/load, mission select overlay, and supply shop. Skill Tree button wired to `showSkillTree()` in v6.89. `_shopRestock()` now calls `saveInventory()` after deducting scrap (v6.95). Dead code removed in v7.20: `if (false)` buy-detail block, `_shopItemTotal()`, `_itemStatCard()`, `_shopSelect()`, and entire loadout-slots subsystem (`showLoadoutSlots`, `saveLoadoutSlot`, `loadLoadoutSlot`, `deleteLoadoutSlot`, `_saveSlot`, `_loadSlot`, `_deleteSlot`, `_closeLoadoutSlots`, `MAX_LOADOUT_SLOTS`, `_getLoadoutSlots`). Three `_showCloudStatusToast()` call sites also removed (stub was in `js/menus.js`). Bug fixed in v7.27: orphaned `${detailHtml}` interpolation in `showShop()` template literal (leftover reference to the removed buy-detail block) caused a `ReferenceError` that prevented `overlay.innerHTML` from being set, leaving the shop overlay blank — removed the stale reference. Stat display labels renamed in v7.28: `modCdPct` label changed from 'CPU Cooldown %' to 'Mod Cooldown %'; `modEffPct` label changed from 'CPU Effect %' to 'Mod Duration %'. `accuracy` removed from `_shopStatNames` in v7.29. `speedPct` display label renamed from 'Speed %' to 'Move Speed %' in v7.30. Shop generation overhauled in v7.39: (1) `SHOP_RARITY_WEIGHTS` const added (Common 45, Uncommon 30, Rare 16, Epic 7, Legendary 2) — deliberately worse odds than field drops (Common 40, Rare 18, Epic 9, Legendary 3) to keep field loot more rewarding; `_rollShopRarity()` uses pure weighted random with no luck/round scaling. (2) `refreshShopStock()` now generates items via the standard `generateItem()` path then forces the shop rarity, re-rolls affixes for that rarity (same pattern as mission rewards), and assigns `legendaryTrait` for applicable Legendary items (weapon, cpu_system, leg_system) — shop items have correct rolled base stats, rarity-appropriate affix counts, and no legacy stats. Item type selection uses empty `enemyData` (`{}`), giving equal weight 10 to all 11 types with no enemy modifiers. (3) Mission-based restocking: `_shopNeedsRestock` flag (default `true`) controls when stock is generated — `showShop()` restocks only when `_shopNeedsRestock` is set; `_deployFromMissionSelect()` sets `_shopNeedsRestock = true` so each new deployment produces fresh stock on the next shop visit; `refreshShopStock()` clears the flag when done; the paid `_shopRestock()` button still calls `refreshShopStock()` directly (forced restock at scrap cost). Eager `refreshShopStock()` call removed from `returnToHangarForMissionSelect()` in `menus.js` — stock is now generated lazily on shop open instead of on every return to mission select. Shop chassis bypass added in v7.55: `refreshShopStock()` now passes `{ isShop: true }` to `generateItem()` so `_selectBaseItem` skips chassis restrictions — the shop stocks all weapon/system types regardless of the player's current chassis. |
| `js/skill-tree-data.js` | `SKILL_TREE_DATA` constant with chassis-keyed skill trees: `light` (193 nodes), `medium` (193 nodes, added v7.06), and `heavy` (193 nodes, added v7.08). Each tree is a JSON array of node objects (id, x, y, t, n, d, s, r, c, i). Added in v6.89. Node text updated in v6.96: all abbreviations in n/d/s fields expanded to full form (FTH→Flamethrower, SG→Shotgun, CD→Cooldown, FR→Fire Rate, DR→Damage Reduction). Updated in v6.98: all node description (d) fields flipped to value-first format (e.g. "+2% Damage" instead of "Damage +2%"); multi-stat descriptions have each part flipped individually. Updated in v6.99: all heat and cooldown reduction stats (SMG/Flamethrower/Siphon/Invis/Barrier/Jump Heat/Cooldown, Mod Cooldown) flipped from negative to positive display (e.g. "-5% SMG Heat" → "+5% SMG Heat") in both d and s fields — these are buffs, not penalties; only the Glass Cannon keystone "-30 All Parts HP" remains negative as a true penalty. Updated in v7.02: `i` (icon) field added to all 168 regular nodes, mapping stat type to icon key (hp, shield, shield_regen, crit, crit_dmg, damage, speed, fire_rate, dodge, dr, mod_cd, augment, smg, flamethrower, shotgun, siphon, heat). Updated in v7.05: Assassin's Eye notable node description updated to "+10% Crit Chance, +20% Crit Damage". Updated in v7.06: medium chassis tree added (193 nodes — Machine Gun, Battle Rifle, Sniper Rifle, Railgun clusters). Updated in v7.08: heavy chassis tree added (193 nodes — Heavy Rifle, Rocket Launcher, Plasma Cannon, Grenade Launcher clusters). Updated in v7.19: `i` field added to all 168 regular nodes in both medium and heavy chassis trees (was missing; light tree already complete since v7.02). Updated in v7.28: OVERCLOCK keystone `d` field changed from "+30% Mod Effects" to "+30% Mod Duration" in all three chassis trees. Updated in v7.31: all "Augment Boost" nodes (48 total across all three chassis trees) converted to "Hull Plating" — `n` changed to "Hull Plating", `d` changed to "+10 All Part HP", `s` changed to "+10 ALL HP" (where present), `i` changed to "hp"; "Augment Effect" is removed as a skill tree stat; `allHP` stat key handles the bonus via the existing `_parseSkillStatString` pattern. |
| `js/skill-tree.js` | Skill tree overlay: `showSkillTree()` / `hideSkillTree()`, pan/zoom SVG canvas (mouse-wheel zoom only), bottom legend, `_renderSkillTree()` (SVG connection lines + hex node polygons + start-node chassis image), hover card (name/rank/description), click-to-allocate/deallocate (`_allocateNode()`), `getSkillTreeBonuses()` with stat-string parser. `_skillTreeState`, `_stNodeMap`, `_skillTreeVB`, `_canDeallocate()`, pan/zoom helpers. Updated in v6.91 (start-node image, hover card redesign, undo-while-open/lock-on-exit). Updated in v6.92: title color changed to #00d4ff; chassis/level sub-text moved into top bar (no separate bar); node colors: allocated regular nodes now cyan (#00d4ff), no "available" visual distinction (all unallocated nodes use locked/dim appearance), connection lines cyan when both allocated else dim; legend updated (AVAILABLE removed, ALLOCATED=cyan, NOTABLE=orange, KEYSTONE=purple). Updated in v6.93: node fills changed to solid dark colors (opaque hex values instead of rgba with low alpha); hover card now shows node description (d field) below the divider instead of stat shorthand (s field) — omitted if empty. `getSkillTreeBonuses()` output merged into loadout overlay gear bonuses panel in v6.94. Auto-save on every node allocation/deallocation via `debouncedCampaignSave()` (v6.95); `hideSkillTree()` also calls `saveCampaignProgress()` on lock-in. Updated in v6.96: hover card splits multi-stat descriptions on commas, each stat on its own line (4-6px gap); `_parseSkillStatString` patterns updated to match expanded weapon/stat names (Flamethrower, Shotgun, Fire Rate, Damage Reduction). Updated in v6.97: keystone and notable node labels now use SVG tspan word wrap to display full names without truncation; font sizes reduced by 1px (keystone: 6→5, notable: 5.5→4.5); lines vertically centered in hex using lineHeight=fontSize×1.3. Updated in v6.98: hover card stat lines starting with "-" rendered in red (#cc2222); relies on value-first description format. Updated in v7.02: `_drawNodeIcon(g, iconType, cx, cy, state)` added — renders a small SVG icon centered in the upper portion of regular hex nodes (y offset −4); rank text moved to lower portion (y offset +7, font-size 5px); allocated icons render at opacity 0.9, locked at 0.5; 15 icon types defined (hp, shield, shield_regen, crit, crit_dmg, damage, speed, fire_rate, dodge, dr, mod_cd, augment, smg, flamethrower, shotgun, siphon, heat). Updated in v7.03: icon group transform changed from `translate(cx,cy)` to `translate(cx,cy) scale(1.5)` — all icons enlarged by 50% for better visibility. Updated in v7.04: HP cross icon path replaced with a properly centered equal-arm cross (`M-1,-3 L1,-3 L1,-1 L3,-1 L3,1 L1,1 L1,3 L-1,3 L-1,1 L-3,1 L-3,-1 L-1,-1 Z`); unallocated node icons now render in muted gray (`rgba(255,255,255,0.18)`) matching the dim node border, while allocated icons retain full vivid colors — achieved via `colorFor()` helper in `_drawNodeIcon`. Updated in v7.06: `showSkillTree()` now reads `_campaignState.chassis` (was `_campaignState.lockedChassis`) to correctly load the matching chassis tree for medium and heavy campaigns. Updated in v7.09: free respec system — all allocated nodes can be de-allocated at any time; left-click = allocate only (`_allocateNode()`), right-click = de-allocate only (`_deallocateNode()`); `_deallocateNode()` calls `_canDeallocate()` for single-rank nodes to block removal that would disconnect other allocated nodes from START, showing a brief status message via `_stShowStatusMsg()` when blocked; nodes with removable ranks show an orange hover highlight (stroke #ff9933); browser context menu suppressed on the SVG canvas. Dead code removed in v7.21: `_lockedAllocations` variable (unused since v7.09 respec system), `_isNodeAvailable()` one-liner wrapper (zero callers — all code calls `_stGetNodeState()` directly), `_skillTreeClampVB()` (zoom handler does clamping inline). Fire Rate stat parsing updated in v7.43: `_parseSkillStatString` now stores `fireRatePct` as `-v` (negated) so skill tree Fire Rate bonuses match the negative=buff internal convention. |
| `js/multiplayer.js` | PVP matchmaking via Socket.IO: connection, remote players, per-frame sync, lobby, PVP HUD, chat, kill feed, respawn, match results, and PVP hangar. |
| `js/events.js` | All top-level global event listeners: resize, click, keydown (all game states), main-menu key nav, player movement/firing, and inventory drag-and-drop. Tremor Legs timer: fires `_triggerTremor` every 500ms (250ms with tlCd) while velocity > 20; resets timer when stationary. `effectiveModCooldown` calculation (v7.24): `fortress_mode` case added — multiplies by `(1 - fmCooldown)` for stackable cooldown reduction perks, then by `0.5` if `heavyDreadnought` is active (dreadnought halving applied multiplicatively after perk reductions). Chassis trait logic added in v7.42: Iron Legs (Heavy) — `_legMult` stays 1.0 when `_legsDestroyed && _ironLegs` (skips 0.5× penalty); Lightweight (Light) — `_lightweightMult` (×1.15) applied when `player.comp.core.hp / max < 0.5`; Agility (Light) — `_agilitySpeedMult` (×1.10) applied when exactly one arm slot is filled. Both Light mults multiply into the `speed` formula. `modCdPct` cooldown mult formula corrected in v7.43: `_gearModCdMult = 1 + (modCdPct/100)` (was `1 -`) so that `modCdPct=-8` yields 0.92× cooldown = 8% shorter. Leg speed penalty fix in v7.47: partial speed penalty `_legHpPct < 0.5 ? (0.5 + _legHpPct) : 1.0` removed from `handlePlayerMovement` — `_legMult` is now 1.0 unless `_legsDestroyed` is true (hp === 0); `_legHpPct` variable removed as unused; penalty only applies when legs are fully destroyed, matching the paper doll black-at-zero visual. |
| `js/init.js` | Game startup: animated grid canvas, callsign handlers, Phaser scene lifecycle (`preload`/`create`/`update`), objective round-end polling, and `window.onload` bootstrap. |

**Script load order (bottom of `<body>`):**
```
phaser.min.js → constants.js → state.js → utils.js → audio.js → mechs.js → cover.js → combat.js → mods.js → perks.js → enemies.js → rounds.js → hud.js → garage.js → menus.js → loot-system.js → enemy-types.js → arena-objectives.js → campaign-system.js → skill-tree-data.js → skill-tree.js → socket.io.min.js → multiplayer.js → events.js → init.js
```

---

## HUD Element IDs Reference

```
slot-L/R/M/S         txt-L/R/M/S          wr-fill-L/R/M/S      wr-st-L/R/M/S
slot-leg-wrap        txt-G
round-hud            round-num            round-remaining       round-kills
boss-hud             boss-bar-fill        boss-bar-name
doll-head            doll-core            doll-lArm             doll-rArm
doll-lShoulder       doll-rShoulder       doll-lLeg             doll-rLeg
minimap-canvas       enemy-doll-hud       edoll-label
stats-overlay        pause-overlay        death-screen          perk-menu
```

---

## Display Conventions

See UI_CONVENTIONS.md for slot label naming (§5), rarity colors (§3), inverted stats (§4), and general color semantics (§3).

---

## Naming Conventions & Patterns

- **Global state variables:** underscore prefix — `_round`, `_perkState`, `_gearState`, `_inventory`, `_equipped`
- **Boolean flags:** `isDeployed`, `isJumping`, `isShieldActive`, `isRageActive`, `_roundActive`, `_extractionActive`
- **Private helpers:** underscore prefix — `_healPlayerFull()`, `_resetHUDState()`, `_cleanupGame()`
- **DOM element IDs:** kebab-case — `round-hud`, `hud-container`, `boss-hud`, `enemy-doll-hud`
- **Slot IDs in garage dropdown:** single uppercase letter — `L` `R` `M` (cpu) `A` (aug) `G` (leg) `S` (shield) `C` (color)
- **Slot keys in `loadout`:** `L` `R` `cpu` `aug` `leg` `shld` (note: `shld` not `shield`)
- **Slot keys in `_equipped`:** different again — `L` `R` `armor` `arms` `legs` `shield` `cpu` `augment`
- **Garage slot ID → loadout key:** use `SLOT_ID_MAP` constant: `{ L:'L', R:'R', M:'cpu', A:'aug', G:'leg', S:'shld' }`. `_equipped` uses a different set: `{ L, R, armor, arms, legs, shield, cpu, augment }`.
- **`selectPerks()`:** pure perk selection (no DOM). Call before `showPerkMenu()` renders cards.
- **`destroyEnemyWithCleanup(scene, e)`:** centralised enemy teardown. Use wherever enemies are force-removed outside the normal damage path.
- **Enemy color palettes:** `ENEMY_COLORS`, `COMMANDER_COLORS`, `MEDIC_COLORS`, `BOSS_COLORS` (all keyed by chassis/type)
- **typeof guards:** External JS functions are always called with `typeof fn === 'function'` guards to survive file load failures
- **`UI_COLORS` const in `js/menus.js`:** mirrors CSS tokens for use in JS template literal inline styles — keeps inline colors consistent with CSS variables without needing `getComputedStyle`
