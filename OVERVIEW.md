# Tech Warrior Online — OVERVIEW

> A browser-based top-down mech shooter built with Phaser 3.60.0. Players choose a chassis, build a loadout in the Hangar, then deploy into wave-based combat. **Warzone** is a roguelike run-and-die loop (`_gameMode='simulation'`); **Campaign** is persistent with XP/levels/missions/shop (`_gameMode='campaign'`); **Multiplayer** is real-time PVP via Socket.IO (`_gameMode='pvp'`).

---

## File Map

| File | Purpose |
|------|---------|
| `index.html` | HTML shell with all screen divs and canonical `<script>` load order; no inline scripts or styles. `boss-hud` is a sibling of `round-hud` (not nested inside it). `objective-hud` anchored `top:12px;left:12px`. `boss-hud` anchored `top:12px` centered. HUD labels: "MOD" for CPU mod row, "SHIELD" for shield row. Shield row name shows equipped loot item name (uppercased) when in campaign; falls back to system name from `loadout.shld` in warzone. `#minimap-wrap` positioned `bottom:15px;right:15px`. |
| `css/base.css` | Design tokens (legacy brand + sci-fi UI palette), `--font-mono`, glow/button geometry tokens, `.tw-btn` system, and universal reset. |
| `css/hud.css` | In-game HUD styles: console frame, paper doll, weapon bar rows, and HUD-specific color tokens. `#hud-container` positioned `bottom:15px;left:15px`. |
| `css/garage.css` | Hangar UI, perk cards, stats panel, dropdown system, and the full `.lo-*` loadout overlay class family. |
| `css/menus.css` | Main menu, pause, callsign, leaderboard, PVP hangar, multiplayer lobby, death screen, warzone perks overlay, drag-and-drop, chassis selector cards, and typography utilities. `.mm-right` uses `justify-content: center`. |
| `js/constants.js` | All immutable game data: chassis (with trait definitions — Light: Dual-Wield/Lightweight/Agility; Medium: Mod Specialist/Shield Specialist/Kill Recharge; Heavy: Improved Armor/Attrition/Iron Legs), weapons (including `siphon` beam), shields (11 total: 4 light, 4 medium including `fortress_shield`, 3 heavy), augments (12 total: none + 11 active), legs (13 total: 5 universal + 3 light + 3 medium + 2 heavy), cover, arenas, enemy color palettes, restriction Sets (`CHASSIS_LEGS`, `CHASSIS_AUGS`, `CHASSIS_WEAPONS`), starter loadouts (new campaigns start with weapon only, shld:'none'), `SLOT_ID_MAP`, `GAME_CONFIG`, `COLOR_OPTIONS` (10 mech colors), and `CROSSHAIR_COLOR_OPTIONS` (8 crosshair colors: White, Red, Green, Cyan, Yellow, Orange, Magenta, Blue). |
| `js/state.js` | All mutable runtime globals: Phaser object refs, game-mode flags, round/combat state, `loadout`, `_perkState`, extraction state, loot pickups, and leaderboard run state. |
| `js/utils.js` | Pure helpers: color utilities, chassis HP calculation, HUD name lookup, and visual FX spawners (damage text, sparks, muzzle flash, debris, footprint). |
| `js/audio.js` | Web Audio API synthesizer with 29+ sound functions, throttle/node-cap system, and first-gesture/tab-visibility lifecycle. Includes siphon beam sounds, `sndLootDrop(rarity)` rarity-scaled landing tones, and `sndScrapPickup()` chirp. |
| `js/mechs.js` | Mech construction, per-frame visual sync, chassis movement effects, rage ghosts, spectre clone logic, and Shield Specialist absorb calculation using `CHASSIS.medium.shieldAbsorbBonus`. |
| `js/cover.js` | Cover placement, battlefield generation, and cover damage/destruction. |
| `js/combat.js` | All weapon firing functions, damage processing, shield absorption, area effects, mine mechanics, siphon beam logic, and keystone trait combat effects. `createExplosion()` supports `isPlayerExplosion` parameter for perk-modified explosions. Siphon beam: per-arm Phaser Graphics with independent heat state and dual-arm support. Layered shield absorption and regen. Tremor Legs AOE. Chassis trait combat effects (Dual-Wield reload mult, Attrition DR, Agility dodge, Shield Specialist regen mult). `fireRatePct` reload formula: `_gearReloadMult = 1 + (fireRatePct/100)` (negative=buff). Kill XP: `_resolveEnemyDeath()` calls `awardKillXP()` in campaign mode with `+N XP` float text in light purple (`#cc99ff`). Keystone implementations: bullet `_wKey` tagging, BULLET HELL (Light: double SMG shot 30ms delay, guarded by `loadout.chassis === 'light'`), LEAD STORM/HEAVY BARRAGE (5th-hit ×2), DEADEYE (consecutive stacking), ARMOR PIERCER (first 2 hits ×1.5), DECIMATOR (SG same-target stacks), PLASMA STORM (impact AOE), BOMBARDMENT fire zones (3s DOT), CARPET BOMB sub-grenades, GUARDIAN ANGEL DR, IRON FORTRESS reflect, EMP OVERLOAD shield disable check. `_bombardmentZones` array with `clearBombardmentZones()`. Cover collision: standard projectiles (SMG/BR/MG/HR/SR/SG/PLSM/FTH) use the group-level `collider(bullets, coverObjects)` registered in `init.js`; `fireRL` registers a per-rocket `collider(rocket, coverObjects)` that triggers explosion on contact; `fireGL` registers a per-ball `collider(ball, coverObjects)` (no callback — physics separation blocks the ball, drag slows it to fuse range); RAIL and SIPHON are hitscan beams that raycast-truncate at cover. `handleBulletEnemyOverlap` guards `!bullet?.active` to prevent double-processing when cover collider fires in the same physics step. |
| `js/mods.js` | All 18 mod-activation functions, drone builder helpers, and augment/leg application. `activateJump()` guards on `_perkState.jumpDisabled`. Mod Specialist (Medium trait): all `_gearModEffMult` calculations multiply by `CHASSIS.medium.modDurationMult` (1.15) for Medium chassis. Keystone mod effects: MISSILE BARRAGE (+3 missiles), BERSERKER (+3s rage), DRONE SWARM (dual drone + damage/duration), EMP OVERLOAD (3s shield disable), IRON FORTRESS (DR 0.50 + reflect + duration). |
| `js/perks.js` | Master perk dictionary, perk pool selection, perk menu render/pick, equip prompt, and per-round perk reset. `_hiddenPerks` Set (11 entries) excludes future perks from the pool. Perk keys must match their `cat` prefix (see CLAUDE.md). |
| `js/enemies.js` | Enemy spawning, full AI state machine, enemy firing, 8 boss spawners, and DOM boss HP bar helpers. |
| `js/rounds.js` | Round init, enemy spawn dispatch, kill tracking, extraction point system, and round banner. `_clearMapForRound()` loot pickup wipe conditional on `_perkState.scorchedEarth`. `clearBombardmentZones()` called at round end and extraction. |
| `js/hud.js` | All HUD update functions: weapon slots, HP/shield bars, paper doll, minimap, cooldown overlays, crosshair, HUD reset. Siphon heat shown inline in weapon bar rows. Layered shield bar with cyan divider at 50%. Paper doll: `getColor()`/`getCol()` return `#000000` (black) at HP ≤ 0, green→yellow→orange→red gradient for HP > 0. `drawCrosshair(g)` reads `loadout.crosshairColor` for color (default `0x00ffff` if unset). |
| `js/garage.js` | Warzone hangar UI: dropdown system, slot selection (dual-wield for light; no duplicate weapons for medium/heavy), and full hangar renderer. Skill-tree stat application uses negative=buff convention. Keystone stat handlers for `modCdPct`, `modDurPct`, `allHPMinus`, `jumpPct`, `barrierSec`. Chassis colour dropdown (slot ID `COL`) shows "CHASSIS" as idle text with a color swatch; crosshair color dropdown (slot ID `XHCOL`) shows "CROSSHAIR" as idle text with a color swatch; both return to their label text after selection. Crosshair saves to `loadout.crosshairColor` and persists to `localStorage` key `tw_crosshair_color`. |
| `js/menus.js` | All menu and overlay logic: main menu, hangar nav, death screen, pause, loadout overlay, hover card system, inventory management, leaderboard, and game cleanup. `populateLoadout()` is the single render entry point. `_renderGearBonusesPanel()` merges gear + skill tree + chassis trait bonuses; CHASSIS TRAITS sub-section uses short stat-style labels (no full descriptions); Medium order: Mod Duration %, Mod Cooldown %, Shield Regen %, Shield Absorb %, Kill Recharge; Medium trait card order: Mod Specialist, Shield Specialist, Kill Recharge. Hover card system: unified stat rows (grey label left, green value right), Legendary trait display (`✦ LEGENDARY TRAIT`), boss unique display (`✦ UNIQUE EFFECT`). Comparison cards: EQUIPPED on left, BACKPACK/SHOP on right, with `CHANGES IF EQUIPPED` diff. `_renderWeaponBar()` shows DMG, DPS, and FR on weapon stat cards; CPU MOD cooldown displayed in seconds. `_equipItemToSlot()` and `_unequipItem()` call `updateBars()` for immediate shield HUD refresh. Cursor restored to `default` in `returnToHangar()`. `_renderChassisSelect()` resets overlay inline styles for centering. `UI_COLORS` const mirrors CSS tokens for inline styles. `returnToHangar()` unconditionally resumes Phaser physics and time (`_isPaused` guard removed — `showDeathScreen()` sets `_isPaused=false` before pausing physics so the guard was always false). `_execDropInTween()` resumes physics/time before starting the drop-in tween so the game world is never left in a paused state during deployment. Campaign loadout top bar has both a mech colour picker (`#lo-color-picker`) and a crosshair colour picker (`#lo-xh-color-picker`); both visible in campaign mode only, called from `populateLoadout()` via `_loRefreshColorSwatch()` and `_loRefreshXhColorSwatch()`; both show "CHASSIS" / "CROSSHAIR" as idle label text with a color swatch beside it. Occupied equipped gear slots and backpack cells receive a rarity-colored background tint (`rd.colorStr + '26'` ≈ 15% alpha) set inline; empty slots have no background tint. |
| `js/loot-system.js` | ARPG loot layer: item generation, rarity/affix system, inventory/equipment management, ground drops (campaign only), unique item effects, campaign cloud save, and save migration arrays. `debouncedCampaignSave()` coalesces saves to one per 500ms. `AFFIX_POOLS` per item type: weapon `[dmgPct, fireRatePct, critChance, critDmg]`, cpu `[coreHP, modEffPct, shieldRegen]`, augment `[dmgPct, fireRatePct, dr, shieldHP, shieldRegen, shieldAbsorb, coreHP, armHP, legHP, modCdPct]`, arms `[fireRatePct, critChance, critDmg]`, armor `[coreHP, armHP, legHP]`, shield `[shieldRegen, shieldAbsorb, dr]`, legs `[speedPct, dodgePct, modCdPct]`. `AFFIX_RANGES` per stat with std/leg ranges. `AFFIX_RANGE_OVERRIDES` for per-type overrides (shield `shieldAbsorb` std:[15,30] / leg:[30,45]). `RARITY_DEFS` with `affixCount` (Common:0 through Legendary:4). Base stats per type: weapon→dmgPct, cpu→modCdPct, armor→dr, arms→armHP, shield→shieldHP [75,125]/[125,175], legs→legHP, augment→random from [fireRatePct, critChance, critDmg, speedPct, dodgePct, modEffPct]. `LEGENDARY_TRAITS` maps types to eligible perk keys. Boss uniques always Legendary with rolled stats + unique effects. Drop system: equal item type weights, tiered rates (Normal 50%, Elite 75%, Boss 100%), rarity weights (Common 40, Uncommon 30, Rare 18, Epic 9, Legendary 3). Scrap coin drops on every kill (gold circle with "S" label, walk-over pickup at 45px). Loot scatter with arc animation, magnet pull at 60px (200px/s, pickup at <15px). Pickup notification pills (left side, rarity-colored, 4px corners, geometric icons via `_iconMap`, 12px font). Item naming: `TIER_PREFIX` + type name. Weapon drops restricted to chassis-equippable via `CHASSIS_WEAPONS` (always filtered). System items filtered for field drops, shop bypasses system filters. |
| `js/enemy-types.js` | Special enemy type definitions (`ENEMY_TYPE_DEFS`), elite modifier system (`ELITE_MODIFIERS`), and all type/modifier lifecycle functions. |
| `js/arena-objectives.js` | Arena layout generators (4 arenas), objective system (4 types), lifecycle functions, and `_arenaState` export. |
| `js/campaign-system.js` | Campaign missions, XP system (kill XP + mission XP), mission modifiers, enemy composition, campaign flow, localStorage save/load, mission select overlay, and supply shop. `XP_PER_KILL` const (normal/medic: 20, commander: 30, elite: 40, boss: 50); `getKillXPMultiplier()` applies level-over-enemy penalty (10% per level over, floor 50%); `getXPForLevel(level)` formula: `100 + (level-2) × 25` increments. Shop: `SHOP_RARITY_WEIGHTS` (Common 45, Uncommon 30, Rare 16, Epic 7, Legendary 2); generates via `generateItem()` with forced rarity; mission-based restock via `_shopNeedsRestock` flag. Shop weapons restricted to chassis-equippable. Buy confirmation prompt with green BUY / CANCEL buttons (auto-width). Tier prefix naming fixed: shop and mission reward paths regenerate `item.name` after forcing rarity. Shop buy slots and sell slots receive a rarity-colored background tint (`color + '26'` ≈ 15% alpha) set inline on the `.lo-slot` div. |
| `js/skill-tree-data.js` | `SKILL_TREE_DATA` constant with chassis-keyed skill trees: `light`, `medium`, and `heavy` (193 nodes each). Node objects (id, x, y, t, n, d, s, r, c, i). All 39 keystone nodes have parseable `s` fields. Hull Plating nodes provide +10 All Part HP. 15 icon types for regular nodes. |
| `js/skill-tree.js` | Skill tree overlay: `showSkillTree()` / `hideSkillTree()`, pan/zoom SVG canvas, `_renderSkillTree()` with hex nodes and SVG icons, hover card, click-to-allocate (left) / deallocate (right) with BFS disconnect check. `getSkillTreeBonuses()` with stat-string parser. `isKeystoneAllocated(nodeId)` helper. Stat parsers: standard stats plus modCdPct, modDurPct, allHPMinus, jumpPct, barrierSec. `fireRatePct` stored negated (negative=buff). Free respec system. Auto-save on allocation changes. |
| `js/multiplayer.js` | PVP matchmaking via Socket.IO: connection, remote players, per-frame sync, lobby, PVP HUD, chat, kill feed, respawn, match results, and PVP hangar. |
| `js/events.js` | All top-level global event listeners: resize, click, keydown, main-menu key nav, player movement/firing, and inventory drag-and-drop. Tremor Legs timer (500ms/250ms). `effectiveModCooldown` with perk reductions, Dreadnought halving, and `_keystoneModCdMult`. Chassis trait movement: Iron Legs (skip 0.5× penalty when destroyed), Lightweight (×1.15 below 50% HP), Agility (×1.10 with one arm). Leg speed penalty only at HP === 0 (`_legsDestroyed`). BERSERKER speed mult during rage. `modCdPct` formula: `1 + (modCdPct/100)` (negative=buff). |
| `js/init.js` | Game startup: animated grid canvas, callsign handlers, Phaser scene lifecycle (`preload`/`create`/`update`), objective round-end polling, and `window.onload` bootstrap. |

**Script load order (bottom of `<body>`):**
```
phaser.min.js → constants.js → state.js → utils.js → audio.js → mechs.js → cover.js → combat.js → mods.js → perks.js → enemies.js → rounds.js → hud.js → garage.js → menus.js → loot-system.js → enemy-types.js → arena-objectives.js → campaign-system.js → skill-tree-data.js → skill-tree.js → socket.io.min.js → multiplayer.js → events.js → init.js
```

---

## HUD Element IDs Reference

```
hud-container        game-canvas
slot-L/R/M/S         txt-L/R/M/S          wr-fill-L/R/M/S      wr-st-L/R/M/S
slot-leg-wrap        txt-G
round-hud            round-num            round-remaining       round-kills
boss-hud             boss-bar-fill        boss-bar-name
doll-head            doll-core            doll-lArm             doll-rArm
doll-lShoulder       doll-rShoulder       doll-lLeg             doll-rLeg
minimap-canvas       enemy-doll-hud       edoll-label
stats-overlay        pause-overlay        death-screen          perk-menu
```

---

## Display Conventions

See UI_CONVENTIONS.md for slot label naming (§5), rarity colors (§3), inverted stats (§4), and general color semantics (§3).

---

## Naming Conventions & Patterns

- **Global state variables:** underscore prefix — `_round`, `_perkState`, `_gearState`, `_inventory`, `_equipped`
- **Boolean flags:** `isDeployed`, `isJumping`, `isShieldActive`, `isRageActive`, `_roundActive`, `_extractionActive`
- **Private helpers:** underscore prefix — `_healPlayerFull()`, `_resetHUDState()`, `_cleanupGame()`
- **DOM element IDs:** kebab-case — `round-hud`, `hud-container`, `boss-hud`, `enemy-doll-hud`
- **Slot IDs in garage dropdown:** single uppercase letter — `L` `R` `M` (cpu) `A` (aug) `G` (leg) `S` (shield) `C` (color) `XHCOL` (crosshair color)
- **Slot keys in `loadout`:** `L` `R` `cpu` `aug` `leg` `shld` (note: `shld` not `shield`) `crosshairColor`
- **Slot keys in `_equipped`:** different again — `L` `R` `armor` `arms` `legs` `shield` `cpu` `augment`
- **Garage slot ID → loadout key:** use `SLOT_ID_MAP` constant: `{ L:'L', R:'R', M:'cpu', A:'aug', G:'leg', S:'shld' }`. `_equipped` uses a different set: `{ L, R, armor, arms, legs, shield, cpu, augment }`.
- **`selectPerks()`:** pure perk selection (no DOM). Call before `showPerkMenu()` renders cards.
- **`destroyEnemyWithCleanup(scene, e)`:** centralised enemy teardown. Use wherever enemies are force-removed outside the normal damage path.
- **Enemy color palettes:** `ENEMY_COLORS`, `COMMANDER_COLORS`, `MEDIC_COLORS`, `BOSS_COLORS` (all keyed by chassis/type)
- **typeof guards:** External JS functions are always called with `typeof fn === 'function'` guards to survive file load failures
- **`UI_COLORS` const in `js/menus.js`:** mirrors CSS tokens for use in JS template literal inline styles — keeps inline colors consistent with CSS variables without needing `getComputedStyle`
